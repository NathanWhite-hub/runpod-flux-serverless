{
  "title": "InvokeAI Serverless",
  "description": "InvokeAI on Runpod Serverless. Boots invokeai-web headless and proxies its queue. Send a graph, get a base64 PNG. Scales to zero. Models load from a volume at INVOKEAI_ROOT.",
  "type": "serverless",
  "category": "image",
  "config": {
    "runsOn": "GPU",
    "containerDiskInGb": 40,
    "gpuCount": 1,
    "gpuIds": "ADA_24,AMPERE_48",
    "allowedCudaVersions": [
      "12.8", "12.7", "12.6", "12.5", "12.4", "12.3", "12.2", "12.1"
    ],
    "env": [
      {
        "key": "INVOKEAI_ROOT",
        "input": {
          "name": "InvokeAI root",
          "type": "string",
          "description": "Data directory holding the model DB and installed models. Point this at a network volume so models persist across cold workers.",
          "default": "/runpod-volume/invokeai",
          "advanced": false
        }
      },
      {
        "key": "INVOKEAI_BOOT_TIMEOUT",
        "input": {
          "name": "Boot timeout (seconds)",
          "type": "number",
          "description": "How long a cold worker waits for invokeai-web to answer before failing.",
          "min": 60,
          "max": 1800,
          "default": 600
        }
      },
      {
        "key": "INVOKEAI_JOB_TIMEOUT",
        "input": {
          "name": "Job timeout (seconds)",
          "type": "number",
          "description": "Maximum wait for a single generation to finish.",
          "min": 30,
          "max": 1800,
          "default": 600
        }
      }
    ]
  }
}
